# Copy this file to .env and fill in your keys.
# .env is only read locally (via python-dotenv) — on PythonAnywhere you'll
# set these as real environment variables instead (see DEPLOY.md).

CRICKETDATA_API_KEY=
YOUTUBE_API_KEY=

# Optional overrides
CACHE_TTL_SECONDS=180
PORT=5050
FLASK_DEBUG=0
